<?xml version="1.0" encoding="UTF-8"?>
<customUI xmlns="http://schemas.microsoft.com/office/2009/07/customui" onLoad="RibbonOnLoad">
  <ribbon>
    <tabs>
      <tab id="tabOleRawDataInserter" label="Figure Package">
        <group id="grpInsert" label="Insert">
          <button id="btnInsertPlotFolder"
                  label="Insert Figure Package"
                  size="large"
                  image="InsertFigurePackageIcon"
                  screentip="Insert plot.png with source files"
                  supertip="Choose a folder containing plot.png. The add-in embeds the whole folder as a zip OLE object and displays plot.png."
                  onAction="RibbonInsertPlotFolder"/>
          <button id="btnInsertImageSupportFiles"
                  label="Insert Image + Files"
                  size="large"
                  image="InsertImageFilesIcon"
                  screentip="Insert an image with selected support files"
                  supertip="Open a dialog where you can review the selected image and support files before embedding the files as a zip OLE object."
                  onAction="RibbonInsertImageSupportFiles"/>
          <button id="btnAttachSupportFilesToSelectedImage"
                  label="Manage Image/OLE Files"
                  size="large"
                  image="ManageImageOleFilesIcon"
                  screentip="Manage files attached to an image or OLE object"
                  supertip="Select an existing picture or Figure Package OLE object, then add or remove embedded files while preserving the display, size, and position."
                  onAction="RibbonAttachSupportFilesToSelectedImage"/>
          <button id="btnShowUsage"
                  label="Usage"
                  size="large"
                  image="UsageHelpIcon"
                  screentip="Show usage instructions"
                  supertip="Learn how to prepare plot.py, plot.png, and raw data before inserting a figure package."
                  onAction="RibbonShowUsage"/>
        </group>
      </tab>
    </tabs>
  </ribbon>
</customUI>
